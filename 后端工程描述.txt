后端工程为Maven+SpringBoot工程，建议使用编译器idea打开项目。
因为云数据库有白名单设置，所以工程并不支持本地调试。
如若想要在本地调试，可修改工程的application.yml文件中的数据库设置，数据库转储文件也在本目录下。
程序部署采用docker部署，pom文件中有Dockerfile的描述信息，同样因为白名单设置，不可随意部署，若想要部署到您的服务器，只需要改一下指定docker的ip以及端口即可。